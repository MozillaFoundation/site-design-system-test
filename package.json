{
  "name": "site-design-system-test",
  "version": "0.0.1",
  "scripts": {
    "build": "node render.js",
    "watch": "npm-run-all --parallel chokidar serve",
    "chokidar": "chokidar 'templates/**/*.njk' 'assets/**/*' -c 'npm run build'",
    "serve": "browser-sync start --server 'docs' --files 'docs/**/*' --port 3100"
  },
  "dependencies": {
    "fs-extra": "^11.1.1",
    "nunjucks": "^3.2.4"
  },
  "devDependencies": {
    "browser-sync": "^3.0.4",
    "chokidar-cli": "^3.0.0",
    "foundation-sites": "^6.9.0",
    "npm-run-all": "^4.1.5"
  }
}
